{% extends 'base.html.twig' %}

{% block title %}Couleur index{% endblock %}

{% block body %}
    <h1>Couleur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for couleur in couleurs %}
            <tr>
                <td>{{ couleur.id }}</td>
                <td>{{ couleur.nom }}</td>
                <td>
                    <a href="{{ path('app_admin_couleur_show', {'id': couleur.id}) }}">show</a>
                    <a href="{{ path('app_admin_couleur_edit', {'id': couleur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_couleur_new') }}">Create new</a>
        <a class="btn btn-primary" href="{{ path('app_admin_vehicule_index') }}"> Retour</a>

{% endblock %}
