
    
    {% if app.user %}
        {% if is_granted('ROLE_ADMIN') %}
            {% set colorName = "#FFC107" %}
        {% else %}
            {% set colorName = "#DAE0E9" %}
        {% endif %}
    {% else %}
        {% set colorName = "#DAE0E9" %}
    {% endif %}




<nav class="navbar navbar-expand-lg navbar-light" style="background-color:{{colorName}};">

    <div class="container-fluid">
        <a class="navbar-brand" href="{{path('app_main')}}"><img class="logo" src="{{ asset('images/logo3.png')}}" alt=""></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="{{path('app_vehicule')}}">Catalogue</a>
            <li class="nav-item dropdown">
            {% if app.user %}
                
            
                
            
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                {% if is_granted('ROLE_ADMIN') %}
                    ADMIN
                {% else %}
                    {{ app.user.prenom }}
                {% endif %}  
                
                
                </a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                    {% if is_granted('ROLE_ADMIN') %}
                        <li><a class="dropdown-item" href="{{ path('app_admin_vehicule_index') }}">
                         Afficher
                            </a>
                         </li>
                    {% endif %}   
                        
                        <li><a class="dropdown-item" href="{{ path('app_profile') }}">Profile</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a></li>
                </ul>
            
        </ul>
       
            {% else %}
     
            <a class="btn btn-light btn-sm mt-1" href="{{path('app_register')}}">Inscription</a>
            <a class="btn btn-light btn-sm mt-1" href="{{path('app_login')}}">Connexion</a>
            {% endif %}
        </div>
    </div>
</nav>
